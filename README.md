# refonte-api

Projet Express.js minimal.

## Démarrage

```bash
npm install
npm run dev
```

## Endpoints

- `GET /` retourne un message de bienvenue.
- `GET /health` retourne l'état de santé de l'API.